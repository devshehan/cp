#include <bits/stdc++.h>

using namespace std;

typedef vector<int> vi;
typedef vector<string> vs;
typedef vector<pair<int,string>> vp;

int main(){

    int a;
    string name;

    cin >> a >> name;

    vp x;
    x.push_back(make_pair(a,name));

    return 0;
}